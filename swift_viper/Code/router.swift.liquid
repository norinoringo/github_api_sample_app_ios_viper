import Foundation
import UIKit

protocol {{ module_info.name }}Router {
    func gotoTop()
}

class {{ module_info.name }}RouterImpl: {{ module_info.name }}Router {
    let viewController: {{ module_info.name }}ViewController

    init(view: {{ module_info.name }}ViewController) {
        viewController = view
    }

    func gotoTop() {
        guard let nextView = R.storyboard.top.topViewController() else { return }
        let configurator = TopConfiguratorImpl()
        configurator.configure(dependency: nextView)
        viewController.navigationController?.pushViewController(nextView, animated: true)
    }
}